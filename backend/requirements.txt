fastapi>=0.111.0
uvicorn>=0.30.1
python-multipart>=0.0.9
python-docx>=1.1.2
pypdf>=4.2.0
edge-tts>=7.2.8
# TTS_PROVIDER=google로 전환할 때만 실제로 쓰인다(개발/기본값은 edge-tts).
google-cloud-texttospeech>=2.16.0
# Chirp3-HD처럼 SSML mark 타임포인팅을 지원하지 않는 Google 음성의 문장
# 경계를 근사할 때, 실제 mp3 길이를 재기 위해 필요하다.
mutagen>=1.47.0
requests>=2.31.0
olefile>=0.46
trafilatura>=1.8.0
pywebpush==2.3.0

# P2: Database & Authentication
supabase>=2.0.0
python-jose[cryptography]>=3.3.0
python-dotenv>=1.0.0
# models.py의 EmailStr이 요구한다. 빠져 있어서 배포 환경에서만
# models import가 실패하고 회원가입/로그인이 500이 났다.
email-validator>=2.0.0
google-auth>=2.25.0
